# Capfile

# Carga las librerías de Capistrano necesarias para el despliegue
load 'deploy'

# Carga las tareas de precompilación de assets si usas Rails 3.1 o superior
load 'deploy/assets' # Comenta o elimina esta línea si no es necesario

# Incluir cualquier otra tarea personalizada que hayas creado
# Dir['vendor/plugins/*/recipes/*.rb'].each { |plugin| load(plugin) } # Comenta o elimina esta línea si no es necesario

# Puedes incluir cualquier archivo de configuración adicional
# load 'config/deploy' # Este archivo se carga automáticamente por defecto
